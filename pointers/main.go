package main

func main() {
	// pointer i think this is nothing more to say
	// in go if we want to define a pointer please use & and *
	// want to more understand about them, let try

	// use * to define a pointer type for variable
	// use & to new pointer for the variable
}
